{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run build && npm run db:migrate --prefix server && npm run start",
    "healthcheckPath": "/api/auth/me",
    "restartPolicyType": "ON_FAILURE"
  }
}
